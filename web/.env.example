# Base URL of the Cowri Rust API, including the /v1 prefix.
# The API only sends CORS credentials headers when the browser Origin matches
# its CORS_ORIGIN env var, so set CORS_ORIGIN=http://localhost:5173 on the API
# when running this app in development.
VITE_COWRI_API_URL=http://localhost:3000/v1
